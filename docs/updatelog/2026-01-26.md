# Update Log (2026-01-26)

## Commits (This PR)
- Branch: `dev/addStylesAndComp`
- Base: `main` (`3408803`)
- `9f769d6` — `addNavBarByMui`
  - Date: `2026-01-26T19:54:10+08:00`
- `61315b1` — `update navbar styles`
  - Date: `2026-01-26T20:13:18+08:00`
- `1a263ef` — `add ctivityDashboard.tsx`
  - Date: `2026-01-26T20:25:32+08:00`

## Summary
Updated the Client UI by moving layout components under `src/app/layout`, introducing an MUI-based `NavBar`, and adding an `ActivityDashboard` that renders fetched activities. This also cleans up the default Vite starter assets/styles and standardizes global styling via `CssBaseline` + Roboto font imports.

## Changes (High Level)
- Client Layout: moved `App.tsx` into `Client/src/app/layout/App.tsx` and wired it to render `NavBar` + `ActivityDashboard`.
- Client UI: added `Client/src/app/layout/NavBar.tsx` implemented with MUI `AppBar/Toolbar` and a gradient background.
- Activities Feature: added `Client/src/features/activities/ActivityDashboard.tsx` to display an activities list using MUI components.
- Styling: moved global CSS import to `Client/src/app/layout/styles.css` and removed the default `App.css`/`index.css` + `react.svg` starter assets.
- Entry Point: updated `Client/src/main.tsx` to import `styles.css` and Roboto fonts for MUI typography.

## Verification (Manual)
1. Start API: `dotnet run --project API/API.csproj`
2. Start Client: `cd Client && npm run dev`
3. Confirm the NavBar renders and the activities list loads from `https://localhost:5001/api/activities`.

